#include <stdio.h>
#include <string.h>

int main()
{
    char str[100];
    printf("enter your word");
    scanf("%s", str);
    if (strcmp(str, "cat") == 0)
    {
        printf("Z");
    } 
    return 0;
}
